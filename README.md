## Bank Transactions CLI

Bank Transactions CLI — консольное приложение для обработки банковских транзакций из различных источников (JSON, CSV, XLSX) 
с возможностью фильтрации, сортировки, маскирования данных и конвертации валют.

Реализованы основы работы с файлами, логирование, декораторы, генераторы, работа с API и построение CLI-интерфейса.

### Возможности

**Программа:**

- отображает количество найденных операций

- считает количество операций по категориям

- выводит список отфильтрованных транзакций

#### Загрузка данных

- Поддерживаются форматы:

  - JSON

  - CSV

  - XLSX (Excel)

- Данные приводятся к единому формату для дальнейшей обработки.

#### Фильтрация транзакций

- По статусу (EXECUTED, CANCELED, PENDING)

- По дате (сортировка по возрастанию / убыванию)

- По валюте

- По ключевому слову в описании (regex)

#### Маскирование данных

- Маска банковской карты:
XXXX XX** **** XXXX

- Маска банковского счета:
**XXXX

#### Конвертация валют

- Поддерживается конвертация:

    - USD → RUB

    - EUR → RUB

- Через внешний API сервиса apilayer (Exchange Rates API).

- API-ключ передается через переменную окружения:
```
API_KEY=your_api_key_here
```

### Архитектурные особенности

- Разделение логики по модулям

- Декоратор @log для логирования работы функций

- Использование генераторов

- Использование Iterator, TypeVar, ParamSpec

- Валидация входных данных

- Работа с pandas (CSV / Excel)

- Централизованное логирование через logging

- Работа с переменными окружения (dotenv)

- Обработка исключений

### Структура проекта

    bank_transactions/
    │
    ├── data/
    │   ├── operations.json
    │   ├── transactions.csv
    │   ├── transactions_excel.xlsx
    │
    ├── logs/
    │
    ├── main.py
    ├── utils.py
    ├── filters.py
    ├── masks.py
    └── ...

### Установка
```
git clone https://github.com/nadezhdapopova-spec/Client_Banking_Widget
cd bank_transactions
pip install -r requirements.txt
```
Для Poetry:
```
poetry install
poetry shell
```
**Запуск**
```
python main.py
```
После запуска отобразится интерактивное меню:
```
1. Получить информацию из JSON
2. Получить информацию из CSV
3. Получить информацию из XLSX
4. Конвертировать сумму в RUB
5. Выйти
```

Пример работы

1. Выбор источника данных
2. Фильтрация по статусу
3. Сортировка по дате
4. Дополнительные фильтры
5. Вывод итогового списка транзакций

### Используемые технологии

Python 3

pandas

requests

python-dotenv

logging

json

re (регулярные выражения)

### Логи

Логи записываются в:

/logs

Фиксируются:

- успешные вызовы функций

- ошибки

- операции маскирования

- чтение файлов

### Тестирование:

Реализованы юнит-тесты, покрытие составляет 99%

Запуск тестов:

```bash
pytest tests/ -v
```

Так как это мой ранний проект, сегодня я бы:

* вынесла CLI-логику в отдельный слой
* добавила dataclass / pydantic для модели транзакции
* написала тесты
* разделила слой ввода-вывода и бизнес-логику
* добавила type-hints более строго
* добавила обработку сетевых ошибок API
* использовала argparse вместо ручного input


### Лицензия:

Проект распространяется под [лицензией MIT](https://github.com/nadezhdapopova-spec/client-banking-widget/blob/main/LICENSE)

### Автор

Надежда Попова

Python Developer

📧 nadezhdapopova13@yandex.ru

🔗 GitHub: nadezhdapopova-spec
